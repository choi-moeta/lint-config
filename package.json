{
  "name": "@moeta/lint-config-monorepo",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/choi-moeta/lint-config.git"
  },
  "scripts": {
    "preinstall": "npx -y only-allow pnpm",
    "lint": "npx eslint --ext .js --fix .",
    "release": "lerna publish --conventional-commits"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
